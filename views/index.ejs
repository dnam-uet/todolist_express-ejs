<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ToDo App</title>
    <!-- <link rel="stylesheet" href="../public/css/style.css"> -->
</head>
<body>
    <div class="container">
        <h2>A Simple ToDo List App</h2>
        <form action="/" method="post">
            <input type="text" name="newTask" placeholder="Add a new task">
            <button formaction="/addtask">Add Task</button>

            <h2>Added Task</h2>
            <%
                for(let i = 0; i < tasks.length; i++){ %>
                    <li><input type="checkbox" name="check" value="<%= tasks[i].id %>" /> <%= tasks[i].task %></li>
                <%}
            %>
            <button formaction="/removetask" type="submit">Remove</button>
            <button formaction="/donetask" type="submit">Done</button>
            <h2>Completed task</h2>
            <%
                for(let i=0; i<done.length; i++) { %>
                    <li><input type="checkbox" name="check" value="<%= done[i].id %>" /> <%= done[i].task %></li>
                    
            <% } %>
        </form>
    </div>
</body>
</html>